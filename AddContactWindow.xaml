<Window x:Class="ContactManager.AddContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ContactManager"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Add contact" Height="300" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="None"
        BorderBrush="Black" BorderThickness="3">

    <Window.Resources>
        <ObjectDataProvider x:Key="GenderList" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:Gender"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <Label Grid.Row="0" HorizontalAlignment="Center" FontSize="24">Add new contact</Label>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Name:"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="1" Content="Surname:"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding Surname, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="2" Content="Email:"/>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="3" Content="Phone:"/>
            <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="4" Content="Gender:"/>
            <ComboBox Grid.Row="4" Grid.Column="1" Margin="5" 
                      ItemsSource="{Binding Source={StaticResource GenderList}}"
                      SelectedItem="{Binding Gender}"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add contact" Width="100" Height="30" Margin="5" Click="AddContact_Click"/>
            <Button Content="Cancel" Width="100" Height="30" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
